#eAV7945677860:AAFVa3CDejo3UNQa3JpvrOFZOBXBmLxE
import uuid
import os
import speech_recognition as sr
import ctypes
import numpy as np
import wave
import pyogg
import numpy
import telebot
import translate

bot = telebot.TeleBot("7597602601:AAHQ2FsmFsGz9YMEyHtqFc2cAFnoIreU4ww",parse_mode=None)
language = 'ru_RU'
r = sr.Recognizer()
translator = translate.Translator(to_lang="ru")


def recognizer(filename):
    with sr.AudioFile(filename) as sourse:
            audio_text = r.listen(sourse)
            try:
                text = r.recognize_google(audio_text, language=language)
                print('перевожу в текст')
                print(text)
                return text
            except:
                print("ошибка")
                return "ошибка"

def convert_opus_to_wav(input_file,ouput_file):
    opus_file = pyogg.OpusFile(input_file)
    channels = opus_file.channels
    samplerate = opus_file.frequency
    buffer_lenght = opus_file.buffer_length
    buffer_ptr = opus_file.buffer

    pcm_data = np.ctypeslib.as_array((ctypes.c_short * buffer_lenght).from_address(ctypes.addressof(buffer_ptr.contents)))
    with wave.open(ouput_file,'wb') as wav_file:
        wav_file.setnchannels(channels)
        wav_file.setsampwidth(2)
        wav_file.setframerate(samplerate)
        wav_file.writeframes(pcm_data.tobytes())







@bot.message_handler(content_types=['voice'])
def voice_proc(message):
    filename = str(uuid.uuid4())
    file_name_full = filename + ".ogg"
    file_name_full_converted = filename + ".wav"
    file_info = bot.get_file(message.voice.file_id)
    dowloaded_file = bot.download_file(file_info.file_path)

    with open(file_name_full, 'wb') as new_file:
        new_file.write(dowloaded_file)




    convert_opus_to_wav(file_name_full,  file_name_full_converted)
    text = recognizer(file_name_full_converted)
    bot.reply_to(message, text)
    os.remove(file_name_full)
    os.remove(file_name_full_converted)
@bot.message_handler(commands=["start","help"])
def command_response(message):
    print(message.chat.username, message.text)
    if message.text == "/start":

        bot.reply_to(message, "привет,я zbot перешли мне голосовое и я его расшифрую")
        if message.text == "/help":
            bot.reply_to(message, "перешли мне голосовое сообщение и я его расшифрую")






@bot.message_handler(content_types=['text' ,])
def text_response(message):
    print(message.chat.username, ":",message.text)
    if message.text == "" or message.text == "я ощущаю себя lovely":
        bot.reply_to(message, "")
    if message.text == "" or  message.text =="" :
        bot.reply_to(message, "")

bot.infinity_polling()













































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































